<% layout("/layouts/boilerplate") %>

<div class="listing-container">
  <div class="listing-details">
 <!-- <div class="row mt-3"> -->
  <div class="col-6 offset-3">
    <h4><%= listing.title %></h4>
  </div> 
   

  <div class="card col-6 offset-3 show-card mb-3 listing-card">
    <img
      src="<%= listing.image.url %>"        class="card-img-top show-img"
      alt="listing-img"
    />
    <div class="card-body">
      <p class="card-text">
        Owned by
        <i><%= listing.owner ? listing.owner.username : "Unknown" %></i>
      </p>
      <p class="card-text"><li><%= listing.description %></li></p>
      <p class="card-text">
        <li>&#8377;<%= listing.price.toLocaleString("en-IN") %></li>
      </p>
      <p class="card-text"><li><%= listing.location %></li></p>
      <p class="card-text"><li><%= listing.country %></li></p>
    </div>
  </div>
  
  <br />
   <!-- <div class="booking-box">
 <% if (currUser && listing.owner &&  listing.owner._id && listing.owner._id.toString() !== currUser._id.toString()) { %>
  <div class="col-6 offset-3 mb-4">
    <div class="card p-3">
      <h5>Book this listing</h5>
      <form action="/bookings/<%= listing._id %>" method="POST">
        <div class="mb-3 novalidate">
          <label for="checkIn" class="form-label">Check-in:</label>
          <input type="date" name="checkIn" id="checkIn" class="form-control" required />
           <div class="invalid-feedback">Please enter the check-in date</div>
        </div>
        
          <label for="checkOut" class="form-label form-control">Check-out:</label>
          <input type="date" name="checkOut" id="checkOut" class="form-control" required />
           <div class="invalid-feedback">Please enter check out date</div>
        
        <button type="submit" class="btn btn-success">Book Now</button>
      </form>
      </div>
    </div>
  </div> -->
  
<!-- <% } else if (!currUser) { %>
  <div class="col-6 offset-3 mb-4">
    <p class="ttext-start m-0">
      <a  href="/login" class="btn btn-dark offset-3">
        Log in</a> to book this listing</p>
  </div>
<% } %>
</div>  -->

  <div class="btns">
    <a
      href="/listings/<%= listing._id %>/edit"
      class="btn btn-dark col-1 offset-3"
      >Edit</a
    >

    <form
      method="POST"
      action="/listings/<%= listing._id %>?_method=DELETE"
      class="d-inline"
    >
      <button class="btn btn-dark offset-1">Delete</button>
    </form>
  </div>

  <div class="col-8 offset-3 mb-3">
    <hr />

    <% if (currUser) { %>
    <h4>Leave a review</h4>
    <form
      action="/listings/<%= listing._id %>/reviews"
      method="POST"
      novalidate
      class="needs-validation">
    
       <div class="mb-3 mt-3">
         <label for="rating" class="form-label">Rating</label>
       <fieldset class="starability-heart">
 
  
  <input type="radio" id="rate1" name="review[rating]" value="1" />
  <label for="rate1" title="Terrible">1 star</label>
  <input type="radio" id="rate2" name="review[rating]" value="2" />
  <label for="rate2" title="Not good">2 stars</label>
  <input type="radio" id="rate3" name="review[rating]" value="3" />
  <label for="rate3" title="Average">3 stars</label>
  <input type="radio" id="rate4" name="review[rating]" value="4" />
  <label for="rate4" title="Very good">4 stars</label>
  <input type="radio" id="rate5" name="review[rating]" value="5" />
  <label for="rate5" title="Amazing">5 stars</label>
</fieldset>
</div> 



      <div class="mb-3 mt-3">
        <label for="comment" class="form-label">Comments</label>
        <textarea
          name="review[body]"
          id="comment"
          cols="30"
          rows="5"
          class="form-control"
          required
        ></textarea>
        <div class="invalid-feedback">Please provide some feedback</div>
      </div>
      <button class="btn btn-outline-dark">Submit</button>
    </form>
    <% } %>

    <p class="mt-4"><b>All Reviews</b></p>
    <div class="row">
      <% if (listing.reviews && listing.reviews.length > 0) { %> <% for (let i =
      0; i < listing.reviews.length; i++) { let review = listing.reviews[i]; %>
      <div class="card col-5 ms-3 mb-3">
        <div class="card-body">
          <h5 class="card-title">
            @<%= review.author && review.author.username ? review.author.username : "Unknown" %>
          </h5>
           <p class="starability-result card-text" data-rating="<%= review.rating %>"> </p> 
           
          <p class="card-text"><%= review.body %></p>
            
 

          <% if (currUser && review.author && currUser._id.toString() ===
          review.author._id.toString()) { %>
          <form
            method="POST"
            action="/listings/<%= listing._id %>/reviews/<%= review._id %>?_method=DELETE"
          >
            <button class="btn btn-sm btn-dark">Delete</button>
          </form>
          <% } %>
        </div>
      </div>
      <% } %> <% } else { %>
      <p>No reviews yet.</p>
      <% } %>
    </div>
    </div>
    </div>
  
  <div class="booking-box col-lg-5 ms-auto">
 <% if (currUser && (listing.owner ||  listing.owner._id.toString() !== currUser._id.toString())) { %>
  <div class="card shadow-sm sticky-top" style="top: 20px;">
    <div class="card-body">
      <h5 class="card-title">Book this listing</h5>
      <form action="/bookings/<%= listing._id %>" method="POST">
       <div class="mb-3">
         <label for="checkIn" class="form-label">Check-in</label>
         <input type="date" id="checkIn" name="checkIn" class="form-control" required>
      </div>

        <div class="mb-3">
           <label for="checkOut" class="form-label">Check-out</label>
           <input type="date" id="checkOut" name="checkOut" class="form-control" required>
        </div>

        <button type="submit" class="btn btn-success">Book Now</button>
      </form>
      </div>
    </div>
   </div>
  
<% } else if (!currUser) { %>
  <div class="col-6 offset-3 mb-4">
    <p class="text-start m-0">
      <a  href="/login?returnTo=<% encodeURIComponent('/listings/' + listing._id) %>" class="btn btn-primary btn-sm px-3">
        Log in</a>
        <span class="text-muted"> to book this listing</p> </span>
  </div>
<% } %>
</div>
</div>    